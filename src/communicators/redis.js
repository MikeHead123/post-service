const Redis = require('ioredis');
const config = require('../config.js');


// const redis = new Redis({
//   port: process.env.DB_HOST || 6379,
//   host: process.env.REDIS_HOST || 'localhost',
// });

// module.exports = redis;
module.exports = {};
